@page "/admin/cabinets"
@attribute [Authorize]
@layout AdminLayout
@inject JsonConfigurationImportService Imports
@inject AdminAuditService Audit
@implements IDisposable

<PageTitle>JSON 柜体配置导入</PageTitle>

<section class="admin-page">
    <div class="admin-page-title">
        <div>
            <h1>JSON 柜体配置导入</h1>
            <p>一次选择一批 JSON 配置文件，每个文件独立生成一个柜体配置，最多同时处理 3 个文件。</p>
        </div>
        <span class="admin-badge model">批量 JSON → 配置</span>
    </div>

    @if (!string.IsNullOrWhiteSpace(message))
    {
        <div class="admin-alert @messageClass">@message</div>
    }

    <section class="admin-panel">
        <div class="admin-panel-title">
            <h2>上传 JSON 配置文件</h2>
            <span>支持 .json</span>
        </div>
        <InputFile OnChange="OnFilesSelected" multiple accept=".json,application/json" />
        @if (selectedFiles.Count > 0)
        {
            <div class="config-import-selected-files">
                @foreach (var file in selectedFiles)
                {
                    <span>@file.Name (@FormatBytes(file.Size))</span>
                }
            </div>
            <div class="admin-actions">
                <button class="admin-button primary" type="button" disabled="@uploading" @onclick="StartImportAsync">
                    @(uploading ? "正在上传…" : $"开始导入（{selectedFiles.Count} 个文件）")
                </button>
                <button class="admin-button" type="button" disabled="@uploading" @onclick="ClearSelection">清空选择</button>
            </div>
        }
        <p class="admin-help">每个 JSON 会独立保存原文件、生成配置和导入前备份；后台刷新或服务重启后仍可查看批次状态。配置文件需包含线号管编号到端子排、端子号的关系。</p>
    </section>

    <section class="admin-panel">
        <div class="admin-panel-title">
            <h2>导入批次</h2>
            <span>共 @batches.Count 个批次</span>
        </div>
        @if (batches.Count == 0)
        {
            <div class="admin-empty">还没有 JSON 配置导入批次。</div>
        }
        else
        {
            <div class="config-import-batch-list">
                @foreach (var batch in batches)
                {
                    <article class="config-import-batch-card">
                        <div class="config-import-batch-heading">
                            <div>
                                <strong>@batch.BatchId</strong>
                                <small>@FormatTime(batch.CreatedAt) · @batch.TotalFiles 个文件 · @GetBatchStatus(batch.Status)</small>
                            </div>
                            <span class="admin-badge @GetBatchBadge(batch.Status)">@batch.CompletedFiles/@batch.TotalFiles 已完成</span>
                        </div>
                        <div class="config-import-progress"><span style="width:@GetBatchProgress(batch)%"></span></div>
                        <div class="config-import-path">批次目录：@batch.RootPath</div>
                        <div class="config-import-file-list">
                            @foreach (var file in batch.Files)
                            {
                                <article class="config-import-file-card">
                                    <div class="config-import-file-heading">
                                        <div>
                                            <strong>@file.OriginalFileName</strong>
                                            <small>@file.CabinetId · @GetFileStatus(file.Status) · @file.ProgressPercent%</small>
                                        </div>
                                        <span class="admin-badge @GetFileBadge(file.Status)">@file.TerminalStripCount 个端子排 / @file.TerminalCount 个端子</span>
                                    </div>
                                    <div class="config-import-progress"><span style="width:@file.ProgressPercent%"></span></div>
                                    @if (!string.IsNullOrWhiteSpace(file.ErrorMessage))
                                    {
                                        <div class="config-import-error">@file.ErrorMessage</div>
                                    }
                                    @if (file.Warnings.Count > 0)
                                    {
                                        <details class="config-import-warnings">
                                            <summary>@file.Warnings.Count 条解析提示</summary>
                                            <ul>
                                                @foreach (var warning in file.Warnings)
                                                {
                                                    <li>@warning</li>
                                                }
                                            </ul>
                                        </details>
                                    }
                                    <div class="config-import-artifacts">
                                        @RenderArtifact("原始 JSON", file.SourceUrl, file.SourcePath)
                                        @RenderArtifact("生成配置", file.ConfigUrl, file.ConfigPath)
                                        @RenderArtifact("备份文件", file.BackupUrl, file.BackupPath)
                                    </div>
                                    @if (file.PreviewRows.Count > 0)
                                    {
                                        <details class="config-import-preview">
                                            <summary>查看端子号–线号管号（前 @Math.Min(file.PreviewRows.Count, 120) 条）</summary>
                                            <div class="admin-table-wrap">
                                                <table class="admin-table">
                                                    <thead>
                                                        <tr>
                                                            <th>端子排</th>
                                                            <th>端子号</th>
                                                            <th>线号管编号</th>
                                                            <th>状态</th>
                                                        </tr>
                                                    </thead>
                                                    <tbody>
                                                        @foreach (var row in file.PreviewRows.Take(120))
                                                        {
                                                            <tr>
                                                                <td>@row.StripCode</td>
                                                                <td>@row.TerminalLabel</td>
                                                                <td>@(row.WireMarkers.Count == 0 ? "—" : string.Join("、", row.WireMarkers))</td>
                                                                <td>@(row.IsExpectedEmpty ? "设计空端子" : "有线号")</td>
                                                            </tr>
                                                        }
                                                    </tbody>
                                                </table>
                                            </div>
                                        </details>
                                    }
                                </article>
                            }
                        </div>
                    </article>
                }
            </div>
        }
    </section>
</section>

@code {
    private readonly List<IBrowserFile> selectedFiles = [];
    private IReadOnlyList<ConfigurationImportBatchRecord> batches = [];
    private PeriodicTimer? refreshTimer;
    private CancellationTokenSource? refreshCancellation;
    private bool uploading;
    private string? message;
    private string messageClass = string.Empty;

    protected override async Task OnInitializedAsync()
    {
        await RefreshAsync();
        refreshCancellation = new CancellationTokenSource();
        refreshTimer = new PeriodicTimer(TimeSpan.FromSeconds(2));
        _ = RefreshLoopAsync(refreshCancellation.Token);
    }

    private void OnFilesSelected(InputFileChangeEventArgs args)
    {
        selectedFiles.Clear();
        selectedFiles.AddRange(args.GetMultipleFiles(100));
        message = null;
    }

    private async Task StartImportAsync()
    {
        if (selectedFiles.Count == 0)
        {
            return;
        }

        uploading = true;
        message = null;
        try
        {
            var uploads = selectedFiles.Select(file => new ConfigurationImportUpload(
                file.Name,
                file.Size,
                file.ContentType,
                cancellationToken => Task.FromResult<Stream>(file.OpenReadStream(25 * 1024 * 1024, cancellationToken)))).ToArray();
            var batch = await Imports.CreateBatchAsync(uploads);
            await Audit.RecordAsync(
                AdminAuditCategories.JsonConfigurationImport,
                "创建 JSON 配置批量导入",
                AdminAuditOutcomes.Success,
                $"已接收 {batch.TotalFiles} 个 JSON 配置文件。",
                batch.BatchId,
                new Dictionary<string, string?>
                {
                    ["文件数量"] = batch.TotalFiles.ToString(System.Globalization.CultureInfo.InvariantCulture)
                });
            selectedFiles.Clear();
            message = $"批次 {batch.BatchId} 已创建，后台开始导入。";
            messageClass = "success";
            await RefreshAsync();
        }
        catch (Exception ex)
        {
            message = ex.Message;
            messageClass = "error";
        }
        finally
        {
            uploading = false;
        }
    }

    private async Task RefreshLoopAsync(CancellationToken cancellationToken)
    {
        if (refreshTimer is null)
        {
            return;
        }

        try
        {
            while (await refreshTimer.WaitForNextTickAsync(cancellationToken))
            {
                await RefreshAsync();
            }
        }
        catch (OperationCanceledException) when (cancellationToken.IsCancellationRequested)
        {
        }
    }

    private async Task RefreshAsync()
    {
        batches = Imports.ListBatches();
        await InvokeAsync(StateHasChanged);
    }

    private void ClearSelection() => selectedFiles.Clear();

    private RenderFragment RenderArtifact(string label, string? url, string path) => builder =>
    {
        builder.OpenElement(0, "span");
        builder.AddContent(1, $"{label}: ");
        if (string.IsNullOrWhiteSpace(url))
        {
            builder.AddContent(2, path);
        }
        else
        {
            builder.OpenElement(3, "a");
            builder.AddAttribute(4, "href", url);
            builder.AddAttribute(5, "target", "_blank");
            builder.AddContent(6, path);
            builder.CloseElement();
        }

        builder.CloseElement();
    };

    private static int GetBatchProgress(ConfigurationImportBatchRecord batch) =>
        batch.TotalFiles == 0 ? 0 : (int)Math.Round(batch.Files.Average(file => file.ProgressPercent));

    private static string GetBatchStatus(ConfigurationImportBatchStatus status) => status switch
    {
        ConfigurationImportBatchStatus.Queued => "排队中",
        ConfigurationImportBatchStatus.Processing => "导入中",
        ConfigurationImportBatchStatus.Completed => "全部完成",
        ConfigurationImportBatchStatus.CompletedWithErrors => "部分失败",
        _ => "失败"
    };

    private static string GetFileStatus(ConfigurationImportFileStatus status) => status switch
    {
        ConfigurationImportFileStatus.Queued => "排队中",
        ConfigurationImportFileStatus.Processing => "导入中",
        ConfigurationImportFileStatus.Completed => "已完成",
        _ => "失败"
    };

    private static string GetBatchBadge(ConfigurationImportBatchStatus status) => status switch
    {
        ConfigurationImportBatchStatus.Completed => "hot",
        ConfigurationImportBatchStatus.Processing => "reconnect",
        ConfigurationImportBatchStatus.CompletedWithErrors => "model",
        ConfigurationImportBatchStatus.Failed => "restart",
        _ => string.Empty
    };

    private static string GetFileBadge(ConfigurationImportFileStatus status) => status switch
    {
        ConfigurationImportFileStatus.Completed => "hot",
        ConfigurationImportFileStatus.Processing => "reconnect",
        ConfigurationImportFileStatus.Failed => "restart",
        _ => string.Empty
    };

    private static string FormatTime(DateTimeOffset value) => value.ToLocalTime().ToString("yyyy-MM-dd HH:mm:ss");

    private static string FormatBytes(long bytes) => bytes switch
    {
        >= 1024 * 1024 => $"{bytes / 1024d / 1024d:0.##} MB",
        >= 1024 => $"{bytes / 1024d:0.##} KB",
        _ => $"{bytes} B"
    };

    public void Dispose()
    {
        refreshCancellation?.Cancel();
        refreshTimer?.Dispose();
        refreshCancellation?.Dispose();
    }
}
